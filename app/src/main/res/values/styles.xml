<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MyMultiAutoCompleteTextViewStyle" parent="@android:style/Widget.AutoCompleteTextView">
        <item name="android:background">@null</item>
        <!-- Otros atributos del estilo -->
    </style>

    <style name="CustomDialogTheme" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">true</item> <!-- Oculta la barra de título -->
        <item name="android:windowActionBar">false</item> <!-- Deshabilita la ActionBar -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>



</resources>